# -*- coding: utf-8 -*-
"""Modular GUI package for Whisper Unified.

Subpackages:
    constants  — single source of truth for colors, sizes, lists, presets
    i18n       — UI translations (en, ru) and runtime language switcher
    widgets/   — reusable widget builders (added in later phases)
    settings_store — persistence to ~/.whisper_gui/settings.json (later phase)
    validators — input validation helpers (later phase)
"""
